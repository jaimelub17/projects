name: 'oura_scorecard'
version: '1.0.0'
config-version: 2

profile: 'oura_scorecard'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  oura_scorecard:
    staging:
      +materialized: view
    marts:
      +materialized: table
