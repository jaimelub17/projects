name: 'corporate_scorecard'
version: '1.0.0'
config-version: 2

profile: 'corporate_scorecard'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  corporate_scorecard:
    staging:
      +materialized: view
    marts:
      +materialized: table
